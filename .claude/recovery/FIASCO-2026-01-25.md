# Doodle Reader Build Fiasco - 2026-01-25

## What Happened

Two Claude sessions were making commits to doodle-reader simultaneously:

1. **Session A (Cloud)**: Added PageSnap (Python OCR service) as a subfolder
2. **Session B (This one)**: Added Newsletter-to-RSS feature

The problem: PageSnap added a Python project inside the repo, and Railway/Nixpacks started trying to build BOTH Node.js and Python, causing build failures.

## Current State

Railway builds are failing with errors like:
- `pip: command not found`
- `undefined variable 'npm'`

The build tries to run: `cd pagesnap && pip install -r requirements.txt`

## Commits from This Session (Newsletter Feature)

```
a026891 Add newsletter-to-RSS feature via Kill the Newsletter
d52bca7 Wire up AddNewsletterModal to Sidebar and App
20ed4d3 Add nixpacks.toml to fix Railway build (DIDN'T FIX IT)
91f5dc2 Fix Railway build: exclude pagesnap from main deployment (STILL BROKEN)
```

**Files created/modified:**
- `convex/newsletters.ts` (NEW) - Backend for Kill the Newsletter integration
- `convex/schema.ts` (MODIFIED) - Added `newsletterFeeds` table
- `components/AddNewsletterModal.tsx` (NEW) - UI modal
- `App.tsx` (MODIFIED) - Wired up modal
- `components/Sidebar.tsx` (MODIFIED) - Added "Add Newsletter" button
- `FEATURE-newsletter-to-rss.md` (NEW) - Feature spec document
- `nixpacks.toml` (NEW) - Attempted fix
- `.railwayignore` (MODIFIED) - Attempted fix

## Patches Saved

The newsletter feature work is saved as git patches in:
- `.claude/recovery/0002-Add-newsletter-to-RSS-feature-via-Kill-the-Newslette.patch`
- `.claude/recovery/0001-Wire-up-AddNewsletterModal-to-Sidebar-and-App.patch`

To apply later: `git am .claude/recovery/*.patch`

## Last Known Good Commit

Before PageSnap and Newsletter work:
```
0f0173d Fix transform output stacking + simplify transcription limits
```

## Recovery Options

### Option 1: Roll back to last good commit (nuclear)
```bash
git reset --hard 0f0173d
git push --force origin main
```
This loses ALL PageSnap and newsletter work. Re-apply patches later.

### Option 2: Fix the build configuration
The issue is Nixpacks detecting Python. Need to either:
- Move pagesnap to a separate repo
- Configure Railway to use a Dockerfile instead of Nixpacks
- Configure Railway dashboard to ignore pagesnap

### Option 3: Use a Dockerfile
Create explicit Dockerfile that only builds Node.js, ignoring pagesnap entirely.

## Recommended Fix

The cleanest solution is to **move pagesnap to its own repo** since it's deployed as a separate Railway service anyway. Then the main doodle-reader repo is pure Node.js.

## For the Other Session

Paste this prompt:

---

**CONTEXT:** We have a build fiasco. Two sessions were committing to doodle-reader simultaneously. The builds are failing because Nixpacks tries to build both the Node.js app AND the pagesnap Python folder.

**READ THIS FILE:** `.claude/recovery/FIASCO-2026-01-25.md`

**THE PROBLEM:** PageSnap is a Python project inside the Node.js repo. Railway detects both and fails.

**RECOMMENDED FIX:** Either:
1. Move pagesnap to a separate GitHub repo (cleanest)
2. Create a Dockerfile that explicitly builds only Node.js
3. Configure Railway dashboard build settings to ignore pagesnap

**DO NOT:** Make more commits trying to fix nixpacks.toml or .railwayignore - those attempts have already failed.

**CURRENT STATE:** Build is broken. Last working commit was `0f0173d`.

---
