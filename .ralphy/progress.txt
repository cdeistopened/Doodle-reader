# Ralphy Progress Log
- [✓] 2026-01-25 03:40 - Add a newsletterFeeds table to convex/schema.ts with fields userId, name, email, feedUrl, createdAt and an index by_user on userId
- [✓] 2026-01-25 03:45 - Create convex/newsletters.ts with: 1) createNewsletterFeed mutation that takes a name string, makes an HTTP POST to https://kill-the-newsletter.com with the name in form data, parses the HTML response to extract the email address and feed URL, and stores them in the newsletterFeeds table; 2) listNewsletterFeeds query that returns all feeds for the current user; 3) deleteNewsletterFeed mutation that removes a feed by ID
- [✓] 2026-01-25 03:48 - Create components/AddNewsletterModal.tsx - a modal component that follows the same styling patterns as other modals in components/ folder. It should have: 1) dark overlay background 2) modal with input field for newsletter name 3) Create Email button that calls the createNewsletterFeed action from convex/newsletters 4) after creation, display the email address with a copy-to-clipboard button 5) Done button to close. Use the same modal patterns as SubscribeModal.tsx or similar.
- [✓] 2026-01-25 03:51 - In App.tsx: 1) Import AddNewsletterModal from components/AddNewsletterModal 2) Add state: const [showNewsletterModal, setShowNewsletterModal] = useState(false) 3) Find where SubscribeModal or Add Feed button is rendered and add a similar Add Newsletter button nearby that sets showNewsletterModal to true 4) Render the AddNewsletterModal component with isOpen={showNewsletterModal} and onClose={() => setShowNewsletterModal(false)}
